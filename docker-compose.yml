version: '3.8'

services:
  userbot:
    build: .
    container_name: telegram_userbot
    restart: always
    stdin_open: true
    tty: true
    volumes:
      - ./app/data:/app/app/data
      - ./sessions:/app/sessions  # New volume for session files
    env_file:
      - .env
